package it.orderflow.control;

/**
 * Events thrown by controllers to other controllers
 */
public enum ControllerEvent {
    ADD_CLIENT,
    ADD_CLIENT_ORDER,
    CLOSE_CLIENT_ORDER,
    EXIT,
    HOME,
    LOGIN,
    LOGOUT,
    MANAGE_ARTICLES,
    MANAGE_CLIENTS,
    MANAGE_CLIENTS_ORDERS,
    MANAGE_EMPLOYEES,
    MANAGE_ORDERS,
    MANAGE_PERSONAL_INFO,
    MANAGE_SUPPLIERS,
    PASSWORD_CHANGE_REQUIRED,
    SUCCESS_EMPLOYEE_UPDATED,
    SUCCESS_LOGIN, MANAGE_PRODUCTS,
}
